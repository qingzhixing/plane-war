# 已知问题记录（临时）

- **HP HUD**：已改为右上角数字显示（如「HP: 3」），不再使用心形图标。

- **双发/散射重复升级覆盖**（待改）
  - 现状：双发与散射为两种独立布尔，同时选会互相覆盖（只生效一种）。
  - 目标：改为「每次射出子弹数 +1」的单一升级类型，可叠加；射出角度根据子弹数量均匀分布（如 1 发居中、2 发左右、3 发左中右等）。

- ~~**玩家移动偶发卡顿**~~（已处理）
  - 处理：单帧拖拽位移限制在 120px 内，避免触摸抖动造成大跳；clamp 后把 `_pointer_pos` 同步为当前屏幕坐标，避免与下一帧拖拽冲突。
